{
  "name": "bs-randomcolor",
  "version": "0.1.0",
  "description": "BuckleScript bindings for randomColor",
  "repository": "git@github.com:ktrzos/bs-randomColor.git",
  "author": "Krzysztof Trzos <k.trzos@jazi.pl>",
  "license": "MIT",
  "keywords": [
    "reason",
    "bucklescript",
    "randomColor",
    "colors"
  ],
  "scripts": {
    "build": "bsb -make-world",
    "start": "bsb -make-world -w",
    "clean": "bsb -clean-world",
    "pretest": "yarn run build",
    "test": "jest",
    "prepublish": "yarn run clean && yarn run build"
  },
  "peerDependencies": {
    "randomcolor": "^0.5.4"
  },
  "devDependencies": {
    "@glennsl/bs-jest": "^0.4.7",
    "bs-platform": "^4.0.18",
    "husky": "^1.3.1",
    "jest": "^24.1.0",
    "randomcolor": "^0.5.4"
  }
}